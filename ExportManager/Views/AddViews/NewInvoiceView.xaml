<local:NewItemViewBase x:Class="ExportManager.Views.AddViews.NewInvoiceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ExportManager.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0, 2, 0, 2">
            <RadioButton Content="Per order"
                         IsChecked="{Binding IsPerOrder}" Margin="2, 0, 2, 0"/>
            <RadioButton Content="Per period"
                         IsChecked="{Binding IsPerPeriod}"/>
        </StackPanel>
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Visible" HorizontalScrollBarVisibility="Disabled">
            <ContentControl  Content="{Binding CurrentViewModel}" Margin="2, 0, 2, 0"/>
        </ScrollViewer>
    </Grid>
</local:NewItemViewBase>
