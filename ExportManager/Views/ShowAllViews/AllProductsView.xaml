<local:AllViewBase x:Class="ExportManager.Views.ShowAllViews.AllProductsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ExportManager.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <DataGrid x:Name="TestDataGrid" AutoGenerateColumns="False" ItemsSource="{Binding List, IsAsync=True}" SelectedItem="{Binding SelectedItem}" IsReadOnly="True" Margin="2,0,2,0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding ProductId}" Width="40"/>
                <DataGridTextColumn Header="Product name" Binding="{Binding Name}" Width="400">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="ToolTip">
                                <Setter.Value>
                                    <ToolTip>
                                        <TextBlock Text="{Binding Remarks}"
                                                   TextWrapping="Wrap"
                                                   Width="400"/>
                                    </ToolTip>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Pot size" Binding="{Binding Potsize}" Width="70"/>
                <DataGridTextColumn Header="Height" Binding="{Binding Height}" Width="100"/>
                <DataGridTextColumn Header="CITES" Binding="{Binding Cites}" IsReadOnly="True" Width="70"/>
                <DataGridTextColumn Header="Weight" Binding="{Binding Weight}" Width="100"/>
                <DataGridTextColumn Header="Color" Binding="{Binding ColorName}" Width="200"/>
                <DataGridTextColumn Header="Color description" Binding="{Binding ColorRemarks}" Width="200"/>
                <DataGridTextColumn Header="Category" Binding="{Binding CategoryName}" Width="200"/>
                <DataGridTextColumn Header="Category Description" Binding="{Binding CategoryRemarks}" MinWidth="100" Width="SizeToCells">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="ToolTip">
                                <Setter.Value>
                                    <ToolTip>
                                        <TextBlock Text="{Binding CategoryRemarks}"
                                                   TextWrapping="Wrap"
                                                   Width="400"/>
                                    </ToolTip>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</local:AllViewBase>
