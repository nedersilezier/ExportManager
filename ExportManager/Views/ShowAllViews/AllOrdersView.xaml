<local:AllViewBase x:Class="ExportManager.Views.ShowAllViews.AllOrdersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ExportManager.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <DataGrid AutoGenerateColumns="False" ItemsSource="{Binding List, IsAsync=True}" SelectedItem="{Binding SelectedItem}">
        <DataGrid.Columns>
            <DataGridTextColumn Header="ID" Binding="{Binding OrderId}" Width="40"/>
            <DataGridTextColumn Header="Client code" Binding="{Binding ClientCode}" Width="200"/>
            <DataGridTextColumn Header="Client name" Binding="{Binding ClientName}" Width="200"/>
            <DataGridTextColumn Header="Country" Binding="{Binding Country}" Width="200"/>
            <DataGridTextColumn Header="Order date" Binding="{Binding OrderDate, StringFormat=d}" Width="100"/>
            <DataGridTextColumn Header="Preparation date" Binding="{Binding PreparationDate, StringFormat=d}" Width="100"/>
            <DataGridTextColumn Header="Shipment date" Binding="{Binding ShipmentDate, StringFormat=d}" Width="100"/>
            <DataGridTextColumn Header="Delivery date" Binding="{Binding DeliveryDate, StringFormat=d}" Width="100"/>
            <DataGridTextColumn Header="Sales person" Binding="{Binding SalesPerson}" Width="200"/>
            <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="200"/>
            <DataGridTextColumn Header="Remarks" Binding="{Binding Remarks}" Width="200"/>
        </DataGrid.Columns>
    </DataGrid>
</local:AllViewBase>
