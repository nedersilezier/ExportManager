<local:AllViewBase x:Class="ExportManager.Views.ShowAllViews.AllCountriesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ExportManager.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <DataGrid AutoGenerateColumns="False" ItemsSource="{Binding List, IsAsync=True}" SelectedItem="{Binding SelectedItem}" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Country name" Binding="{Binding Name}" Width="250"/>
                <DataGridTextColumn Header="Country code" Binding="{Binding ISO2Code}" Width="150"/>
                <DataGridTextColumn Header="Phone code" Binding="{Binding PhoneCode}" Width="150"/>
                <DataGridTextColumn Header="EU member" Binding="{Binding IsEUMemberToText}" Width="75" IsReadOnly="True"/>
                <DataGridTextColumn Header="Description" Binding="{Binding Remarks}" Width="700"/>
                <DataGridTextColumn Header="Last updated" Binding="{Binding UpdatedAt}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</local:AllViewBase>
