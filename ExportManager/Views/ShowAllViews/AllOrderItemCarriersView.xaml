<local:AllViewBase x:Class="ExportManager.Views.ShowAllViews.AllOrderItemCarriersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ExportManager.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="0, 2, 0, 2" Background="AliceBlue">
        <Grid.RowDefinitions>
            <RowDefinition Height="24"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="24"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="7*"/>
            <ColumnDefinition Width="10*"/>
            <ColumnDefinition Width="40"/>
        </Grid.ColumnDefinitions>
        <Border  TextBlock.BaselineOffset="5" Grid.Row="0" Grid.Column="0" BorderBrush="LightGray" Background="LightGray" BorderThickness="0,0,0,1" Margin="0, 0, 1, 1">
            <TextBlock Text="Carriers" FontSize="14" FontWeight="Medium" Margin="1, 1, 1, 1" HorizontalAlignment="Center"/>
        </Border>
        <Border  TextBlock.BaselineOffset="5" Grid.Row="0" Grid.Column="1" BorderBrush="LightGray" Background="LightGray" BorderThickness="0,0,0,1" Margin="1, 0, 0, 1">
            <TextBlock Text="Assigned order items" FontSize="14" FontWeight="Medium" Margin="1, 1, 1, 1" HorizontalAlignment="Center"/>
        </Border>
        <DataGrid Grid.Row="1" Grid.Column="0" Grid.RowSpan="4" AutoGenerateColumns="False" ItemsSource="{Binding List, IsAsync=True}" SelectedItem="{Binding SelectedItem}" IsReadOnly="True" Margin="0, 0, 1, 0" BorderBrush="LightGray">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Carrier ID" Binding="{Binding CarrierId}" Width="70"/>
                <DataGridTextColumn Header="Type" Binding="{Binding CarrierType}" Width="100"/>
                <DataGridTextColumn Header="Shelves" Binding="{Binding AmountOfShelves}" Width="100"/>
                <DataGridTextColumn Header="Extensions" Binding="{Binding AmountOfExtensions}" Width="100"/>
                <DataGridTextColumn Header="Amount of plants" Binding="{Binding AmountOfPlants}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
        <DataGrid Grid.Row="1" Grid.Column="1" AutoGenerateColumns="True" ItemsSource="{Binding AssignedOrderItems, IsAsync=True}" SelectedItem="{Binding SelectedAssignedOrderItem}" IsReadOnly="True" Margin="1, 0, 0, 1" BorderBrush="LightGray">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Line No" Binding="{Binding InternalNo}" Width="70"/>
                <DataGridTextColumn Header="Order item ID" Binding="{Binding OrderItemId}" Width="85"/>
                <DataGridTextColumn Header="Product name" Binding="{Binding FullProductName}" Width="400"/>
                <DataGridTextColumn Header="Tray" Binding="{Binding TrayType}" Width="60"/>
                <DataGridTextColumn Header="Quantity" Binding="{Binding Quantity}" Width="80"/>
                <DataGridTextColumn Header="Quality" Binding="{Binding Quality}" Width="70"/>
                <DataGridTextColumn Header="Stock ID" Binding="{Binding StockItemId}" Width="70"/>
            </DataGrid.Columns>
        </DataGrid>
        <Border  TextBlock.BaselineOffset="5" Grid.Row="2" Grid.Column="1" BorderBrush="LightGray" Background="LightGray" BorderThickness="0,0,0,1" Margin="1, 1, 0, 1">
            <TextBlock Text="Available order items" FontSize="14" FontWeight="Medium" Margin="1, 1, 1, 1" HorizontalAlignment="Center"/>
        </Border>
        <DataGrid Grid.Row="3" Grid.Column="1" Grid.RowSpan="2" AutoGenerateColumns="False" ItemsSource="{Binding AvailableOrderItems, IsAsync=True}" SelectedItem="{Binding SelectedAvailableOrderItem}" IsReadOnly="True" Margin="1, 1, 0, 0" BorderBrush="LightGray">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Line No" Binding="{Binding InternalNo}" Width="70"/>
                <DataGridTextColumn Header="Order item ID" Binding="{Binding OrderItemId}" Width="85"/>
                <DataGridTextColumn Header="Product name" Binding="{Binding FullProductName}" Width="400"/>
                <DataGridTextColumn Header="Tray" Binding="{Binding TrayType}" Width="50"/>
                <DataGridTextColumn Header="Quantity" Binding="{Binding Quantity}" Width="80"/>
                <DataGridTextColumn Header="Quality" Binding="{Binding Quality}" Width="70"/>
                <DataGridTextColumn Header="Stock ID" Binding="{Binding StockItemId}" Width="70"/>
            </DataGrid.Columns>
        </DataGrid>
        <Grid Grid.Row="0" Grid.Column="2" Grid.RowSpan="4" Background="GhostWhite" Margin="1, 0, 0, 0">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <Button Margin="0, 2, 0, 2" Height="32" Width="28" Command="">
                    <Image Source="/Icons/top-arrow.png"/>
                </Button>
                <Button Margin="0, 5, 0, 2" Height="32" Width="28" Command="">
                    <Image Source="/Icons/down-arrow.png"/>
                </Button>
            </StackPanel>
        </Grid>
    </Grid>
    
</local:AllViewBase>
