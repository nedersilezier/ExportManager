<local:AllViewBase x:Class="ExportManager.Views.ShowAllViews.AllOrderItemCarriersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ExportManager.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="0, 2, 0, 2">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <DataGrid Grid.Row="0" AutoGenerateColumns="False" ItemsSource="{Binding List, IsAsync=True}" SelectedItem="{Binding SelectedItem}" IsReadOnly="True" Margin="0, 0, 0, 0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Carrier ID" Binding="{Binding CarrierId}" Width="300"/>
                <DataGridTextColumn Header="Carrier type" Binding="{Binding CarrierType}" Width="700"/>
                <DataGridTextColumn Header="Shelves amount" Binding="{Binding AmountOfShelves}" Width="*"/>
                <DataGridTextColumn Header="Shelves amount" Binding="{Binding AmountOfShelves}" Width="*"/>
                <DataGridTextColumn Header="Extensions amount" Binding="{Binding AmountOfExtensions}" Width="*"/>
                <DataGridTextColumn Header="Amount of batches" Binding="{Binding AmountOfBatches}" Width="*"/>
                <DataGridTextColumn Header="Amount of plants" Binding="{Binding AmountOfPlants}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
        <Border Grid.Row="1" Background="AliceBlue"/>
        <DataGrid Grid.Row="2" AutoGenerateColumns="True" ItemsSource="{Binding AssignedOrderItems, IsAsync=True}" SelectedItem="{Binding SelectedAssignedOrderItem}" IsReadOnly="True" Margin="0, 0, 0, 0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Line No" Binding="{Binding InternalNo}" Width="50"/>
                <DataGridTextColumn Header="ID" Binding="{Binding OrderItemId}" Width="50"/>
                <DataGridTextColumn Header="Product name" Binding="{Binding FullProductName}" Width="400"/>
                <DataGridTextColumn Header="Tray" Binding="{Binding TrayType}" Width="50"/>
                <DataGridTextColumn Header="Quantity" Binding="{Binding Quantity}" Width="80"/>
                <DataGridTextColumn Header="Quality" Binding="{Binding Quality}" Width="70"/>
                <DataGridTextColumn Header="Stock ID" Binding="{Binding StockItemId}" Width="70"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
    
</local:AllViewBase>
