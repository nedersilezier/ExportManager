<UserControl x:Class="ExportManager.Views.ReportViews.InvoiceReportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ExportManager.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Views/MainWindowResources.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid Background="White" Margin="2, 2, 2, 2">
        <Grid.RowDefinitions>
            <RowDefinition Height="35"/>
            <RowDefinition Height="1"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="1"/>
            <RowDefinition Height="35"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Label Grid.Row="0" Content="Invoice summary" HorizontalContentAlignment="Center" Margin="0, 2, 0, 2"/>
        <Border Grid.Row="1"  Background="LightGray"/>
        <Grid Grid.Row="2" Margin="2, 2, 2, 2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="150"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="0" VerticalAlignment="Center">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="300"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="40"/>
                    <RowDefinition Height="40"/>
                </Grid.RowDefinitions>
                <Label Grid.Row="0" Grid.Column="0" Content="Select date" HorizontalAlignment="Center" Margin="0, 2, 0, 2"/>
                <DatePicker Grid.Row="0"  Grid.Column="1" SelectedDate="{Binding Date}" Margin="2, 2, 2, 2" VerticalAlignment="Center" Height="30"/>
                <Label Grid.Row="1" Grid.Column="0" Content="Select invoice" HorizontalAlignment="Center" Margin="0, 2, 0, 2"/>
                <ComboBox Grid.Row="1" Grid.Column="1" Margin="2, 2, 2, 2"
  ItemsSource="{Binding InvoiceComboBoxItems}"
  SelectedItem="{Binding SelectedInvoice}"
  VerticalContentAlignment="Center">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Value}"/>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>
            </Grid>
            <Button Grid.Column="1" Content="Calculate" Command="{Binding CalculateInvoice}" Width="150" Margin="2, 2, 2, 2" Style="{StaticResource CustomButtonStyle}"/>
            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="40"/>
                    <RowDefinition Height="1"/>
                    <RowDefinition Height="40"/>
                    <RowDefinition Height="1"/>
                    <RowDefinition Height="40"/>
                    <RowDefinition Height="1"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="1"/>
                    <ColumnDefinition Width="200"/>
                </Grid.ColumnDefinitions>
                <Label Grid.Row="0" Grid.Column="0" Content="Net total:" FontWeight="DemiBold" VerticalAlignment="Bottom" HorizontalAlignment="Center" Margin="0, 2, 0, 2"/>
                <Label Grid.Row="0" Grid.Column="2" Content="{Binding NetTotal}" VerticalAlignment="Bottom" HorizontalAlignment="Left" Margin="20, 2, 2, 2"/>
                <Border Grid.Row="1" Grid.ColumnSpan="3" Background="LightGray"/>
                <Label Grid.Row="2" Grid.Column="0" Content="Total tax:" FontWeight="DemiBold" VerticalAlignment="Bottom" HorizontalAlignment="Center" Margin="0, 2, 0, 2"/>
                <Label Grid.Row="2" Grid.Column="2" Content="{Binding TaxTotal}" VerticalAlignment="Bottom" HorizontalAlignment="Left" Margin="20, 2, 2, 2"/>
                <Border Grid.Row="3" Grid.ColumnSpan="3" Background="LightGray"/>
                <Label Grid.Row="4" Grid.Column="0" Content="Gross total:" FontWeight="DemiBold" VerticalAlignment="Bottom" HorizontalAlignment="Center" Margin="0, 2, 0, 2"/>
                <Label Grid.Row="4" Grid.Column="2" Content="{Binding GrossTotal}" VerticalAlignment="Bottom" HorizontalAlignment="Left" Margin="20, 2, 2, 2"/>
            </Grid>
        </Grid>
        <Border Grid.Row="3"  Background="LightGray"/>
        <Label Grid.Row="4" Content="Invoice items" HorizontalContentAlignment="Center" Margin="2, 2, 2, 2"/>
        <DataGrid Grid.Row="5" ItemsSource="{Binding Path=InvoiceItemsList}" AutoGenerateColumns="False" Margin="2, 0, 2, 0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Item No" Binding="{Binding ItemNo}" Width="80"/>
                <DataGridTextColumn Header="Name" Binding="{Binding FullName}" Width="*"/>
                <DataGridTextColumn Header="Quantity" Binding="{Binding Quantity}" Width="100"/>
                <DataGridTextColumn Header="Net amount" Binding="{Binding NetAmount}" Width="100"/>
                <DataGridTextColumn Header="Tax amount" Binding="{Binding TaxAmount}" Width="100"/>
                <DataGridTextColumn Header="Gross amount" Binding="{Binding GrossAmount}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
