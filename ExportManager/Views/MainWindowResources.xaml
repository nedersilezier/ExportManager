<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:vm="clr-namespace:ExportManager.ViewModels"
                    xmlns:vw="clr-namespace:ExportManager.Views">
    <HierarchicalDataTemplate x:Key="BasicDataTemplate" ItemsSource="{Binding Children}">
        <TextBlock Text="{Binding DisplayName}" />
    </HierarchicalDataTemplate>
    
    <!--<HierarchicalDataTemplate x:Key="BasicDataTemplate" ItemsSource="{Binding Children}">
        <MenuItem Header="{Binding DisplayName}" Command="{Binding Command}" />
    </HierarchicalDataTemplate>-->
    <!--<DataTemplate DataType="{x:Type vm:WszystkieTowaryViewModel}">
        <vw:WszystkieTowaryView/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type vm:NowyTowarViewModel}">
        <vw:NowyTowarView/>
    </DataTemplate>-->
    <!--Main functions-->
    <DataTemplate DataType="{x:Type vm:AllProductsViewModel}">
        <vw:AllProductsView/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type vm:AllOrderItemsViewModel}">
        <vw:AllOrderItemsView/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type vm:AllBatchesViewModel}">
        <vw:AllBatchesView/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type vm:AllOrdersViewModel}">
        <vw:AllOrdersView/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type vm:AllInvoicesViewModel}">
        <vw:AllInvoicesView/>
    </DataTemplate>
    <!--Dictionary tables-->
    <DataTemplate DataType="{x:Type vm:AllCarrierTypesViewModel}">
        <vw:AllCarrierTypesView/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type vm:AllCategoriesViewModel}">
        <vw:AllCategoriesView/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type vm:AllColorsViewModel}">
        <vw:AllColorsView/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type vm:AllCountriesViewModel}">
        <vw:AllCountriesView/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type vm:AllCultivationsViewModel}">
        <vw:AllCultivationsView/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type vm:AllPaymentMethodsViewModel}">
        <vw:AllPaymentMethodsView/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type vm:AllQualitiesViewModel}">
        <vw:AllQualitiesView/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type vm:AllTrayTypesViewModel}">
        <vw:AllTrayTypesView/>
    </DataTemplate>
    <!--Clients, Growers...-->
    <DataTemplate DataType="{x:Type vm:AllAddressesViewModel}">
        <vw:AllAddressesView/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type vm:AllClientsViewModel}">
        <vw:AllClientsView/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type vm:AllGrowersViewModel}">
        <vw:AllGrowersView/>
    </DataTemplate>
    <!--Creating new items-->
    <DataTemplate DataType="{x:Type vm:NewColorViewModel}">
        <vw:NewColorView/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type vm:NewCategoryViewModel}">
        <vw:NewCategoryView/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type vm:NewProductViewModel}">
        <vw:NewProductView/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type vm:NewCarrierTypeViewModel}">
        <vw:NewCarrierTypeView/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type vm:NewCountryViewModel}">
        <vw:NewCountryView/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type vm:NewCultivationViewModel}">
        <vw:NewCultivationView/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type vm:NewPaymentMethodViewModel}">
        <vw:NewPaymentMethodView/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type vm:NewQualityTypeViewModel}">
        <vw:NewQualityTypeView/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type vm:NewTrayTypeViewModel}">
        <vw:NewTrayTypeView/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type vm:NewAddressViewModel}">
        <vw:NewAddressView/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type vm:NewClientViewModel}">
        <vw:NewClientView/>
    </DataTemplate>
    <!--Own button style-->
    <Style x:Key="CustomButtonStyle" TargetType="Button">
        <Setter Property="Foreground" Value="Black"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border" 
                            Background="{TemplateBinding Background}" 
                            BorderBrush="DarkGray" 
                            BorderThickness="1" 
                            CornerRadius="1"
                            Margin="4"
                            Padding="4"
                            Width="100"
                            Height="32">
                        <ContentPresenter HorizontalAlignment="Center" 
                                      VerticalAlignment="Center"
                                      RecognizesAccessKey="True"
                                      ContentSource="Content"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="border" Property="BorderBrush" Value="Black"/>
                            <Setter TargetName="border" Property="BorderThickness" Value="1"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="LightBlue"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="FontWeight" Value="DemiBold"/>
            </Trigger>
        </Style.Triggers>
    </Style>
    <Style x:Key="MainHCCStyle" TargetType="{x:Type HeaderedContentControl}">
        <Setter Property="HeaderTemplate">
            <Setter.Value>
                <DataTemplate>
                    <Border 
            Background="LightGray" 
            BorderBrush="DarkGray" 
            BorderThickness="1" 
            CornerRadius="5" 
            Margin="4" 
            Padding="4" 
            SnapsToDevicePixels="True" 
            >
                        <TextBlock 
              FontSize="14"
              FontWeight="Bold"
              Foreground="Black" 
              HorizontalAlignment="Center"  
              Text="{TemplateBinding Content}" 
              />
                    </Border>
                </DataTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="IsTabStop" Value="False" />
        <Setter Property="Template">
            <Setter.Value>
                <!-- 
        This template ensures that content of a HeaderedContentControl 
        fills the available vertical space. 
        -->
                <ControlTemplate TargetType="{x:Type HeaderedContentControl}">
                    <DockPanel>
                        <ContentPresenter 
              DockPanel.Dock="Top"
              ContentSource="Header" 
              ContentTemplate="{TemplateBinding HeaderTemplate}" 
              />
                        <ContentPresenter 
              ContentSource="Content" 
              ContentTemplate="{TemplateBinding ContentTemplate}" 
              />
                    </DockPanel>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--
  This style ensures that the borders in the main window are consistent.
  -->
    <Style x:Key="MainBorderStyle" TargetType="{x:Type Border}">
        <Setter Property="Background" Value="WhiteSmoke" />
        <Setter Property="BorderBrush" Value="LightGray" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="5" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
    </Style>

    <!--
  This template explains how to render the list of commands on the left
  side in the main window (the 'Control Panel' area).
  -->
    <DataTemplate x:Key="CommandsTemplate">
        <ItemsControl IsTabStop="False" ItemsSource="{Binding}" Margin="6,2">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <TextBlock Margin="2,6">
            <Hyperlink Command="{Binding Path=Command}">
              <TextBlock Text="{Binding Path=DisplayName}" />
            </Hyperlink>
          </TextBlock>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </DataTemplate>
    <!-- 
  This template explains how to render 
  a tab item with a close button.
  -->
    <DataTemplate x:Key="ClosableTabItemTemplate">
        <DockPanel Width="120">
            <Button 
        Command="{Binding Path=CloseCommand}"
        Content="X"
        Cursor="Hand"
        DockPanel.Dock="Right"
        Focusable="False"
        FontFamily="Courier" 
        FontSize="9"
        FontWeight="Bold"  
        Margin="0,1,0,0"
        Padding="0"
        VerticalContentAlignment="Bottom"
        Width="16" Height="16" 
        />
            <ContentPresenter 
        Content="{Binding Path=DisplayName}" 
        VerticalAlignment="Center" 
        />
        </DockPanel>
    </DataTemplate>

    <!--
  This template explains how to render the 'Workspace' content area in the main window.
  -->
    <DataTemplate x:Key="WorkspacesTemplate">
        <TabControl 
      IsSynchronizedWithCurrentItem="True" 
      ItemsSource="{Binding}" 
      ItemTemplate="{StaticResource ClosableTabItemTemplate}"
      Margin="4"
      />
    </DataTemplate>
</ResourceDictionary>