<UserControl x:Class="ExportManager.Views.VolumeReportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ExportManager.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="35"/>
            <RowDefinition Height="1"/>
            <RowDefinition Height="37"/>
            <RowDefinition Height="35"/>
            <RowDefinition Height="35"/>
            <RowDefinition Height="35"/>
            <RowDefinition Height="35"/>
        </Grid.RowDefinitions>
        <Label Grid.Row="0" Content="Volume per order per period" HorizontalContentAlignment="Center" Margin="0, 2, 0, 2"/>
        <Border Grid.Row="1"  Background="LightGray"/>
        <Grid Grid.Row="2" Margin="0, 2, 0, 2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="150"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Label Grid.Column="0" Content="Select client" HorizontalAlignment="Center" Margin="0, 2, 0, 2"/>
            <ComboBox Grid.Column="1" Margin="2, 2, 2, 2"
                      ItemsSource="{Binding ClientsItems}"
                      SelectedItem="{Binding SelectedClient}"
                      VerticalContentAlignment="Center">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Value}"/>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
        </Grid>
        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="150"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="150"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Label Grid.Column="0" Content="From" HorizontalAlignment="Center" Margin="0, 2, 0, 2"/>
            <DatePicker Grid.Column="1" SelectedDate="{Binding FromDate}" Margin="2, 2, 2, 2" VerticalAlignment="Center" Height="30"/>
            <Label Grid.Column="2" Content="To" HorizontalAlignment="Center" Margin="0, 2, 0, 2"/>
            <DatePicker Grid.Column="3" SelectedDate="{Binding ToDate}" Margin="2, 2, 2, 2" VerticalAlignment="Center" Height="30"/>
        </Grid>
        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="150"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Label Grid.Column="0" Content="Select unit" HorizontalAlignment="Center" Margin="0, 2, 0, 2"/>
            <ComboBox Grid.Column="1" Margin="2, 2, 2, 2"
                      ItemsSource="{Binding CarrierTypesItems}"
                      SelectedItem="{Binding SelectedCarrierType}"
                      VerticalContentAlignment="Center">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Value}"/>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
        </Grid>
        <Button Grid.Row="5" Content="Calculate" Command="{Binding CalculateVolume}" Width="150" Margin="2, 2, 2, 2"/>
        <Grid Grid.Row="6">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="150"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Label Grid.Column="0" Content="Calculated volume" HorizontalAlignment="Center" Margin="0, 2, 0, 2"/>
            <Label Grid.Column="1" Content="{Binding Volume}" HorizontalAlignment="Left" Margin="20, 2, 2, 2"/>
        </Grid>
    </Grid>
</UserControl>
